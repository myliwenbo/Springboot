server:
  port: 8082

spring:
  application:
    name: eureka-client    #客户端名称

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/  #服务端的ULR

## 安全认证的配置
#security:
#  basic:
#    enabled: true  # 2.0 之后 安全配置将不再是可定制的